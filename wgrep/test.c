#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int get_substring(char* string, char* buffer, size_t start, size_t end) {
    
}

int main() {
    char string[] = "Hello"; 
    printf("%lu\n", strlen(string)); 
}